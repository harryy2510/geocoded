# Skills

No project-specific skills are currently defined.

Use this folder only for reusable workflows that are specific to this repo. Each skill should live at `.agents/skills/<skill-id>/SKILL.md` with `name` and `description` frontmatter.
